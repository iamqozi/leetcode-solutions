#include <vector>

class Solution {
public:
    std::vector<int> twoSum(std::vector<int>& nums, int target) {
        // TODO: Find two distinct indices whose values sum to target.
        // This is an unfinished starter, not an accepted solution.
        return {};
    }
};
